# Changelog

## 0.1.5

- Republish under new npm package name

## 0.1.4

- Add Development section to README
- Fix CI badge to reference publish.yml
- Add test script to package.json

## 0.1.0
- Initial release
- `createRouter()` with `add()`, `match()`, and `routes()` methods
- Path parameter extraction (`:param` syntax)
- Query string stripping on match
